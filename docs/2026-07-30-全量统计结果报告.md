# DeepModeling 全量贡献者统计 - 结果报告

日期：2026-07-30
统计区间：2026-01-01 ~ 2026-07-30（含两端，UTC）
口径：`--include-forks self`，不统计代码行数，bot 与 AI 助手均保留在名单中仅作标注

本次一趟跑完全部 34 个仓库（含 sciencepedia），零失败，结果在 `output_all/`。
所有待确认项均已收到答复并落实，本报告不再列出悬而未决的问题。

## 一、总体数字

| 指标 | 数值 |
|---|---|
| 贡献者 | 364 |
| 处理仓库 | 34 |
| 跳过仓库 | 33 |
| 失败仓库 | 0 |
| 无账号身份 | 47 |
| 剔除的 `[bot]` 账号 | 4 |

六项贡献合计：提交 9478 次、PR 创建 1858、PR 合并 1291、PR 评审 2590、
issue 创建 761、issue 评论 2755。

364 人中 354 人是自然人，其余 10 个账号标注为推断 bot（6 个）或 AI 助手（4 个）。
另有 121 人提交数为零但有评审或 issue 贡献，即 Q6 确认要纳入的 maintainer 类贡献。

id 中显式带 `[bot]` 后缀的 4 个账号不进主表，只写入 bots.csv；靠名字推断的
机器人（njzjz-bot、codecov 等）保留在主表并标注。这两类的处理均已确认。

## 二、输出了哪些文件

全部在 `output_all/` 一个目录里。

| 文件 | 内容 | 什么时候看它 |
|---|---|---|
| summary.csv | 跨仓库汇总，一人一行，按提交数降序，364 行 | 出表彰名单看这个 |
| by_repo.csv | 一人一仓库一行，522 行，最细粒度 | 想知道某人在哪个仓库贡献了多少 |
| repos/<仓库名>.csv | 按仓库拆分的 33 个文件 | 分发给各项目 maintainer 核对 |
| contributors.md | 与 summary.csv 同样内容的 Markdown 表格 | 直接贴到文档或公示页 |
| contributors.json | 结构化数据，含统计口径元信息 | 给程序消费，或做二次加工 |
| unmatched.csv | 47 条无 GitHub 账号的身份，按提交数降序 | 需要人工认人时，见第四节 |
| bots.csv | 机器人账号按仓库拆分为 96 行，含已剔除的 4 个 `[bot]` 账号 | 核对有无真人被误标、或找回被剔除的量 |
| ai_assisted.csv | AI 助手提交追溯到的指派人，19 行 | 想知道 Copilot 的提交是谁指派的 |
| run_meta.json | 运行参数、跳过与失败清单、API 消耗、追溯覆盖率 | 复核这次跑的口径与完整性 |

CSV 都带 UTF-8 BOM，Excel 直接打开中文不乱码。

summary.csv 的列：repo（汇总表里恒为空，仅与分仓表共用列头）、login、name、
email、github_url、commits、commits_not_in_upstream、pr_created、pr_merged、
pr_reviewed、issue_created、issue_commented、is_fork、upstream、upstream_family、
is_bot、is_ai_assistant。

六个维度不要直接相加当"总贡献量"：同一个 PR 会同时记进 pr_created 与
pr_merged，加起来是重复计数。排名请挑单一主维度，通常用 commits。

## 三、名单前列长什么样

排除 bot 与 AI 后的前 12 位（提交 / PR 创建 / 评审 / issue 创建）：

| 账号 | 提交 | PR 创建 | 评审 | issue |
|---|---|---|---|---|
| wanghan-iapcm | 1307 | 129 | 303 | 13 |
| mohanchen | 872 | 86 | 350 | 10 |
| njzjz | 518 | 129 | 362 | 365 |
| dyzheng | 411 | 36 | 5 | 10 |
| OutisLi | 368 | 77 | 40 | 20 |
| Xuwznln | 319 | 7 | 0 | 0 |
| Growl1234 | 216 | 37 | 5 | 4 |
| anyangml | 170 | 40 | 6 | 0 |
| qg-phy | 153 | 17 | 4 | 2 |
| Critsium-xy | 145 | 38 | 28 | 24 |
| iProzd | 135 | 14 | 131 | 0 |
| Cstandardlib | 133 | 45 | 10 | 19 |

各仓库贡献量前列：abacus-develop 180 人 3494 次、deepmd-kit 70 人 3313 次、
Uni-Lab-OS 37 人 914 次、DeePTB 16 人 276 次、dpdata 31 人 232 次、
dpdispatcher 16 人 161 次。

## 四、结果解读时需要注意的四点

以下四点都是已确认的处理方式，不是待决问题，但看表时需要知道。

### 1. mohanchen 已合并三个邮箱

上一版里 `abacus_fixer` 以无账号身份占据第二位，750 次提交。原因是提交
里的邮箱 `mohanchen@pku.eud.cn` 把 `edu` 误拼为 `eud`，自动归并认不出来。
按确认已加入人工映射，现与 `mohanchen@pku.edu.cn`、
`mohan.chen.chen.mohan@gmail.com` 合为一行，合计 872 次提交（119 + 750 + 3），
仍居第二位但已是实名。unmatched.csv 中不再有这条。

人工映射写在 `contributors/identity.py` 的 `MANUAL_EMAIL_LOGIN`，目前只有这一条，
每条都带可核验的依据注释。这是唯一绕过自动判定的入口，新增需谨慎。

### 2. njzjz 相关身份按确认保持拆开

同属 Jinzhe Zeng 名下但分列四处，确认为仅作参考、不合并：

| 身份 | 提交 | 位置 |
|---|---|---|
| njzjz（本人） | 518 | 主表 |
| njzjz-bot | 556 | 主表，is_bot=True |
| njzjz.bot@gmail.com（同一 bot，裸邮箱未关联账号） | 230 | unmatched.csv |
| OpenClaw 系列三个邮箱 | 12 | unmatched.csv |

bot 侧合计 798 次提交。当前口径是 bot 归 bot、人归人，两边不混，
所以本人的 518 次不含这 798 次。

### 3. 六个推断 bot 保留在主表

njzjz-bot（两行，556 与 230）、codecov、dependabot、github-actions、pre-commit-ci。
后四个在本窗口内提交数均为 0，只有评审或 issue 活动。这些账号 id 里没有
`[bot]` 后缀，靠名字推断，有误判真人的风险，因此保留在表内只作标注 ——
筛 `is_bot=True` 即可排除。

已剔除的 4 个是 github-actions[bot]、pre-commit-ci[bot]、dependabot[bot]、
coderabbitai[bot]，合计约 3692 次提交，全在 bots.csv 里可查。
bots.csv 中也出现 Copilot，那是分仓明细里 name 侧带后缀的记录，
与主表的 Copilot 是同一账号的两种呈现，不影响主表数字。

`.github` 仓库窗口内的提交几乎全部来自 github-actions[bot]，剔除后贡献者归零，
所以 repos/ 下是 33 个文件而非 34 个，`.github` 进入了处理流程但不单独出文件。

### 4. 四个 AI 账号已标注

| 账号 | 主要贡献 |
|---|---|
| Copilot | 351 次提交 |
| copilot-pull-request-reviewer | 416 次评审 |
| copilot-swe-agent | 28 个 PR |
| claude | 2 次提交 |

按确认保留在主表并标注，筛 `is_ai_assistant=True` 即可排除。
要纯自然人名单，同时排除 is_bot 与 is_ai_assistant 两列，剩 353 人。

## 五、数据本身的两处局限

这两处是方法上的固有限制，不是待办事项。

### AI 指派人只追溯到约一半

带 AI 共同作者标记的提交共 390 次，追溯到指派人 186 次，未追溯到 204 次，
覆盖率 48%。未追溯到的多是 squash 合并后原始作者信息丢失的情况，无法补全。

ai_assisted.csv 按（仓库, 指派人）分行，同一人跨多仓有多行，
看某人总数需自行累加。已追溯到的前列：njzjz 125 次、link89 25 次、
anyangml 12 次、HydrogenSulfate 9 次、Cstandardlib 7 次。
`guess` 列为空表示未追溯到账号，不能当作已确认。

### 47 条身份关联不到 GitHub 账号

这些提交的邮箱既不是 GitHub noreply 地址，也没在 API 侧出现过，
`guess` 列是工具的猜测，不保证正确。最大几条：njzjz.bot@gmail.com 230 次
（见第四节第 2 点）、Fei Yang 60 次、zdcao 32 次、huangming 29 次、
xiaoyan 25 次（sciencepedia 唯一贡献者）、Tianju Xue 21 次。
合计占总提交量的比例很小，不影响名单主体。

## 六、33 个跳过仓库的构成

组织共 67 个仓库，处理 34 个，跳过 33 个。跳过分三类：

| 原因 | 数量 |
|---|---|
| pushed_at 早于统计起点 | 23 |
| fork 上游为 external，按 `--include-forks=self` 排除 | 6 |
| fork 上游为 tooling，按 `--include-forks=self` 排除 | 4 |

external 六个：ADMP、GPUMD、fealpy、lammps、msys、plumed2。
tooling 四个：abacus-code-reviewer、argo-workflows、deepmd-kit-recipes、
get-changed-files —— 这些是别人写的通用 CI 组件的 fork，统计会把上游作者
算成本组织贡献者，故整仓排除。

要注意 `should_skip` 中 fork 判断先于 pushed_at 判断，所以一个仓库同时满足
两个条件时只记录 fork 那条原因。实际查这 10 个 fork 的 pushed_at，
只有 GPUMD（2026-06-09）落在窗口内，其余 9 个最后推送都在 2024–2025 年。
也就是说 fork 类里唯一因分类判断而未纳入的活跃仓库是 GPUMD，
另外 9 个 fork 无论如何调整分类都跑不出窗口内数据。lammps 与 plumed2
都在这 9 个之列（最后推送均为 2024-04-11），即便改判为社区项目也不会改变结果。

### sciencepedia 已纳入，体积上限默认取消

sciencepedia 的 GitHub 标称体积 17.5 GB，此前被 `--max-repo-size` 的默认
2048 MB 挡掉。这是误伤：工具用 `--filter=blob:none` 做无 blob 克隆，只取
提交元数据、不下载文件内容，该仓库在 `.cache/` 里的实际占用是 412 MB，
约为标称体积的 2.3%。默认值已改为 0（不限），参数保留给磁盘确实紧张时手动设限。

该仓库窗口内只有 1 位贡献者：xiaoyan（xiaoyan@dp.tech），25 次提交，
无 GitHub 账号，已记入 unmatched.csv。

## 七、口径说明

- 统计区间 2026-01-01 ~ 2026-07-30，UTC，含两端，按 author date 过滤
- fork 仓库只统计上游为本组织的（`--include-forks self`）；
  判断 fork 实际投入用 commits_not_in_upstream 列，不要用 commits
- 不统计代码行数
- self 组织白名单五个：deepmodeling、abacusmodeling、dptech-corp、
  deepflamecfd、mzjb
- 不设仓库体积上限（`--max-repo-size 0`，即新默认），sciencepedia 因此纳入
- 本次 API 点数消耗为 0，全部命中 `.cache/` 缓存

测试 283 项全部通过（`pytest -m "not network"`），其中 5 项覆盖人工映射机制，
3 项覆盖体积上限的新默认与显式设限行为。
