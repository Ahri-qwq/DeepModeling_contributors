# 任务交接文档

日期：2026-07-29
项目：`C:\MyCode\tools\github_contributors`
分支：`master`（已从 worktree 合并回来，worktree 已清理）
状态：工具已完整可用，237 个测试通过。等待用户确认业务口径后跑全量。

## 一、给接手的 Claude：先做这三件事

1. 读本文档全部内容。
2. 读 `docs/2026-07-29-fork清单待确认.md`，用户昨天带走这份去确认 fork
   分类，今天会带答复回来。
3. 跑 `python -m pytest -m "not network" -q` 确认 237 passed。若失败，
   先排查再动手，不要在红灯状态上继续开发。

不要重复已完成的工作（下方第三节列明）。不要重新爬取数据，缓存已有 70 MB。

## 二、这个项目在做什么

爬取 DeepModeling 组织下全部仓库的贡献者名单，用于社区感谢与表彰。
统计 commit 数、PR（创建/合并/评审）、issue（创建/评论），输出
CSV / Markdown / JSON 三种格式。

完整设计与用法见 `README.md`，实现计划见
`docs/superpowers/plans/2026-07-28-github-contributors.md`。

## 三、已完成的工作

计划中的 Task 1–9 全部实现完毕。此外修复了 4 个在真实数据上才暴露的缺陷，
这些缺陷单元测试都测不出来，值得知道它们的成因以免重蹈。

### 提交历史（近 8 条）

| 提交 | 内容 |
|---|---|
| 620808e | docs: Q1 补充 GPUMD 实测例子，按修复后数据更正数字 |
| a0f6a99 | fix: fork 中上游可达的提交不再被误记为本社区贡献 |
| f52e9fc | docs: 更新 README 与待确认问题，补充文件引用与实测数字 |
| c1cfa60 | feat: unmatched.csv 输出姓名与贡献量，便于人工认人 |
| b38f764 | fix: 修复 --no-fetch 联网崩溃，并按姓名合并被拆开的身份 |
| 5a8bdd1 | feat: 实现主流程编排与端到端集成测试 |
| 1277d64 | fix: 时间窗改按 author date 在 Python 侧过滤 |
| 4d09a57 | feat: 实现 CSV/Markdown/JSON 三种格式输出 |

### 修过的四个缺陷（不要改回去）

一、时间窗过滤（`1277d64`）。`git log --since/--until` 过滤的是
committer date，而本项目口径是 author date，两者在 rebase/squash 场景可
相差数月。且 `--since` 会截断遍历：分支顶端若有旧日期提交，其祖先中窗口
内的提交会被整片漏掉。现改为 git 侧取全量、Python 侧按 author date 筛。
git 没有按 author date 过滤的选项，`--since-as-filter` 也是 committer
date 口径。

二、`--no-fetch` 联网崩溃（`b38f764`）。该模式声称零网络，但仍调用
`fetch_repos` 拉仓库清单，而该模式不取 token，空 token 请求返回 401 直接
崩溃。现把清单纳入缓存。注意：原集成测试 monkeypatch 掉了 `fetch_repos`，
恰好绕过会崩的路径而掩盖了问题 —— 这类 patch 要警惕。

三、身份被拆成两行（`b38f764`）。未在 GitHub 登记的邮箱查不到 login，
同一人被拆开。实测 Han Wang 拆成 1151+110、dyzheng 拆成 128+278，榜首
根本没显示出来。现有 `merge_by_name` 在同仓库内按姓名合并，规则保守：
姓名完全相同、且只对应一个 login 才合并。

四、fork 上游排除失效（`a0f6a99`）。`build_rows` 原用
`upstream_counts.get(email, gs.commits)` 兜底，但
`count_upstream_excluded` 只列出上游不可达的人。某人提交全来自上游时他
不在字典里，兜底反而把 `not_in_upstream` 抬成 `commits`，与设计意图相反。
实测 GPUMD 的上游作者 Zheyong Fan 显示 207/207，实际应为 207/0。现改为
fork 缺失即记 0，非 fork 才等于 commits。同时用 `None` 区分「上游对比
失败，无从判断」与「对比成功但无人独立于上游」，前者退化为 commits。

## 四、当前数据产出

`output_final/` 是最新一次抽查（修复全部生效后生成），可直接使用。
`output_gpumd/` 是 GPUMD 单独测试，作为 fork 口径例子的证据来源。

抽查范围：deepmd-kit、abacus-develop、dpdispatcher 三个仓库，
窗口 2026-01-01 ~ 2026-07-29。

| 指标 | 数值 |
|---|---|
| 贡献者 | 236 位 |
| 待确认身份 | 26 条（unmatched.csv） |
| bot 账号 | 8 个不同账号（bots.csv 按仓库拆分为 20 行） |
| 零提交但有评审/issue 贡献 | 81 人 |
| PR 创建 / PR 评审合计 | 1010 / 2105 |

各仓库：abacus-develop 178 位 3465 次（3462 次不在上游）、
deepmd-kit 64 位 2807 次、dpdispatcher 11 位 97 次。

榜首 wanghan-iapcm 1261 次（自动合并 1151+110 的结果），
dyzheng 406 次（合并 128+278），njzjz 365 次并有 268 次评审、141 次
提 issue。Copilot 295 次排第 6，目前按用户裁决保留在主表。

缓存 `.cache/` 70 MB，含 4 个仓库的 mirror 与 API 响应。

## 五、等待用户确认的事项

### 优先级最高：fork 分类（用户今天带答复回来）

见 `docs/2026-07-29-fork清单待确认.md`。要点：

组织下 67 个仓库、17 个 fork。按 2026-01-01 起的窗口，只有 5 个 fork 会
真正进入统计，其余 12 个最后推送在 2025-05 及更早，工具会以「pushed_at
早于统计起点」直接跳过。

窗口内 5 个的工具判定：abacus-develop、dpgen2、deepflame-dev、dflow 为
`self`（纳入），GPUMD 为 `external`（排除）。用户口述的口径是「只有
GPUMD 算外部项目」，与工具判定一致。

有一处待澄清：用户提到 lammps 和 plumed2 应算社区项目，但工具判为
`external`。本次统计不受影响（都在窗口外），但若这是真实分歧，需要用户
给出判断依据 —— 它们的上游 `lammps/lammps`、`plumed/plumed2` 是独立的
知名开源项目，账号不属 DeepModeling 生态。若社区与上游有正式协作关系，
可把账号加入 `contributors/repos.py` 的 `SELF_ORGS` 白名单。

确认后无需重爬，`--include-forks self` 即符合口径。

### 其余八项（见 `docs/2026-07-29-待确认问题.md`）

用户已把该文档连同 summary.csv、unmatched.csv 发给 3 位社区管理成员，
等待答复。Q1 fork 口径与上面那份 fork 清单重叠。

| 编号 | 问题 | 当前实现 |
|---|---|---|
| Q1 | fork 是否纳入表彰 | 建议纳入 self、排除 external |
| Q2 | sciencepedia（17.6 GB）是否纳入 | 默认被 `--max-repo-size 2048` 挡掉 |
| Q3 | 正式统计区间 | 待定，抽查用的是 2026-01-01 起 |
| Q4 | author date 还是 committer date | 已实现 author date，用户上一轮选定 |
| Q5 | 代码行数是否纳入评价 | 默认关闭 |
| Q6 | 非代码贡献如何计入 | 已分列输出五项 |
| Q7 | 身份映射人工确认 | abacus_fixer 749 次疑似 mohanchen |
| Q8 | AI 助手（Copilot）是否计入 | 用户裁决保留在主表 |
| Q9 | bot 是否有误判 | 8 个账号待核对 |

Q7 最关键：`abacus_fixer`（邮箱 `mohanchen@pku.eud.cn`，把 edu 拼成
eud，故查不到账号）749 次提交，疑似就是 mohanchen。若确认，其实际提交数
约 867 次而非当前显示的 118 次。姓名不同，自动规则刻意不合并。

## 六、下一步该做什么

用户带回 fork 确认后，按以下顺序推进。

一、若 fork 分类需要修正，改 `contributors/repos.py` 的 `SELF_ORGS` 或
`TOOLING_UPSTREAMS`，补测试后重跑。不要改 `classify_upstream` 的判定
逻辑本身，它按上游账号归属分类，设计是对的。

二、跑全量。命令：

```
python -m contributors --org deepmodeling --since <确认的起始日> --include-forks self --out-dir output_full
```

预计 40 余个仓库、1 至 2 小时、2 至 3 GB。用 `run_in_background` 跑，
不要占着前台等。跑完检查 `run_meta.json` 的 `failures` 与 `skipped`。

三、若 Q7 确认了身份映射，当前没有手工映射表机制。可以考虑加一个
`identity_map.csv` 让用户手工填 email→login，这是之前讨论过但未实施的
方案（用户当时选了按姓名自动合并）。做之前先问用户要不要。

## 七、工作方式约定

用户要求全程中文思考与回答。Markdown 表格单元格内禁止用 `**` 加粗，
表格外正文可以。这两条写在用户的全局 CLAUDE.md 里。

每个 task 结束必须 commit，message 用中文，格式 `<type>: <描述>`，
并带 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`。

测试命令统一 `pytest -m "not network"`。改动生产代码前先写失败测试
（项目一直按 TDD 走，四个缺陷都是先写失败测试再修的）。

遇到真实数据与预期不符时，用 systematic-debugging 流程查根因再修，
不要猜。上述四个缺陷全部是这样查出来的，其中两个的根因与最初猜测不同。

## 八、几个容易踩的坑

Windows 环境。终端输出中文会显示为乱码，但文件内容正常，加
`PYTHONIOENCODING=utf-8` 可缓解。`git log` 的字段分隔符不能用 `\x00`
（CreateProcess 不允许参数含 NUL），项目用的是 `\x1f`。删除 git 缓存
目录不能用 `shutil.rmtree(ignore_errors=True)`，pack 文件带只读位会
残留空壳目录，项目用 `onexc` 回调清只读位。

Excel 占用文件会让写 CSV 报 PermissionError。若遇到，让用户关掉窗口，
或临时输出到别的目录。

`--no-fetch` 跳过 GitHub API，PR/issue 列全为 0 且 email→login 映射
缺失（实测 236 人掉到 175 人，仅 41 人能关联账号）。它只适合验证 commit
口径，出正式名单必须走完整跑法。

权限分类器偶尔不可用，报「temporarily unavailable」。等一下重试即可，
不是代码问题。

